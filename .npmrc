# CTEA Platform - npm/pnpm Configuration
# Enforces Node.js version requirements

# Strict engine checking - prevents installation on incompatible Node.js versions
engine-strict=true

# Use exact versions for better reproducibility
save-exact=true

# Prefer offline packages when available
prefer-offline=true
